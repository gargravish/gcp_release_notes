#!/usr/bin/env node

// This script is no longer needed since we're using relative paths for assets
// It's kept for reference only

console.log('Using relative paths for assets - no need to set base URL.');
process.exit(0); 